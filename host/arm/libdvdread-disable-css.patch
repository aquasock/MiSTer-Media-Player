diff --git a/src/dvd_input.c b/src/dvd_input.c
index 8b654dc..0caef2b 100644
--- a/src/dvd_input.c
+++ b/src/dvd_input.c
@@ -425,5 +425,12 @@ static int file_close(dvd_input_t dev)
 int dvdinput_setup(void *priv, dvd_logger_cb *logcb, dvd_type_t dvda_flag)
 {
+#ifdef MMP_DISABLE_DVDCSS
+  (void)dvda_flag;
+  DVDReadLog(priv, logcb, DVD_LOGGER_LEVEL_WARN,
+             "Encrypted DVD support deliberately disabled by MediaPlayer.");
+  dvdinput_setup_builtin(priv, logcb);
+  return 0;
+#else
   void *dvdcss_library = NULL;
 
 #ifdef HAVE_DVDCSS_DVDCPXM_H
@@ -563,6 +570,7 @@ int dvdinput_setup(void *priv, dvd_logger_cb *logcb, dvd_type_t dvda_flag)
     dvdinput_setup_builtin(priv, logcb);
     return 0;
   }
+#endif
 }
 
 /**
